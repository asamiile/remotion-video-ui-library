import {zColor} from "@remotion/zod-types";import {z} from "zod";
export const parallaxAnalysisStackSchema=z.object({title:z.string(),status:z.string(),primaryColor:zColor(),secondaryColor:zColor(),layerCount:z.number().int().min(2).max(8),depthOffset:z.number().min(0).max(240),speed:z.number().min(0).max(4),gaugeValue:z.number().min(0).max(100)});export type ParallaxAnalysisStackProps=z.infer<typeof parallaxAnalysisStackSchema>;export const parallaxAnalysisStackDurationFrames=240;
export const defaultParallaxAnalysisStackProps={title:"ANALYSIS LAYER",status:"TRACKING ACTIVE",primaryColor:"#32eaff",secondaryColor:"#82ffc9",layerCount:5,depthOffset:46,speed:1,gaugeValue:68} as const;export const parallaxAnalysisStackPatterns:Record<string,ParallaxAnalysisStackProps>={faceTrack:{...defaultParallaxAnalysisStackProps},machineScan:{...defaultParallaxAnalysisStackProps,title:"MACHINE SCAN",status:"CORE STABLE",primaryColor:"#ffad32",secondaryColor:"#fff1a6",layerCount:7,depthOffset:58,speed:.7,gaugeValue:84}};
