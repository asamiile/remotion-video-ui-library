import React from "react";import {AbsoluteFill,useCurrentFrame} from "remotion";import type {KaleidoscopeMirrorProps} from "./kaleidoscope-mirror.schema";
export const KaleidoscopeMirrorTemplate:React.FC<KaleidoscopeMirrorProps>=({backgroundColor,primaryColor,secondaryColor,segments,rotationSpeed,pulseAmount,radius})=>{const frame=useCurrentFrame();const pulse=1+Math.sin(frame/16)*pulseAmount;return <AbsoluteFill style={{backgroundColor}}><svg viewBox="0 0 1920 1080" width="100%" height="100%"><g transform={`translate(960 540) rotate(${frame*rotationSpeed*.35}) scale(${pulse})`} style={{filter:`drop-shadow(0 0 18px ${primaryColor})`}}>{Array.from({length:segments},(_,i)=><g key={i} transform={`rotate(${i*360/segments}) scale(${i%2?-1:1} 1)`}><path d={`M0 0 C${radius*.18} ${-radius*.16},${radius*.66} ${-radius*.1},${radius} 0 C${radius*.65} ${radius*.18},${radius*.25} ${radius*.24},0 0Z`} fill={i%2?`${primaryColor}55`:`${secondaryColor}55`} stroke={i%2?primaryColor:secondaryColor} strokeWidth="2"/><circle cx={radius*.67} cy="0" r="18" fill={secondaryColor}/></g>)}</g></svg></AbsoluteFill>};
