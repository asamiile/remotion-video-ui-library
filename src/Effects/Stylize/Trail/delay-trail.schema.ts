import {zColor} from "@remotion/zod-types";import {z} from "zod";
export const delayTrailSchema=z.object({primaryColor:zColor(),secondaryColor:zColor(),trailCount:z.number().int().min(2).max(24),spacingPx:z.number().min(2).max(100),orbitRadius:z.number().min(50).max(700),objectSize:z.number().min(10).max(240),speed:z.number().min(0).max(4),fade:z.number().min(.1).max(1)});export type DelayTrailProps=z.infer<typeof delayTrailSchema>;export const delayTrailDurationFrames=180;
export const defaultDelayTrailProps={primaryColor:"#38efff",secondaryColor:"#c7fff5",trailCount:12,spacingPx:18,orbitRadius:320,objectSize:72,speed:1,fade:.72} as const;export const delayTrailPatterns:Record<string,DelayTrailProps>={orbitalEcho:{...defaultDelayTrailProps},amberSweep:{...defaultDelayTrailProps,primaryColor:"#ff9d2e",secondaryColor:"#fff0ad",trailCount:18,orbitRadius:430,speed:1.5,fade:.58}};
