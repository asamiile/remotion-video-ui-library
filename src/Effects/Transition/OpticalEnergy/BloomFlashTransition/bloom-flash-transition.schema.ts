import {zColor} from "@remotion/zod-types";import {z} from "zod";
export const bloomFlashTransitionSchema=z.object({coreColor:zColor(),glowColor:zColor(),centerX:z.number().min(0).max(100),centerY:z.number().min(0).max(100),peakFrame:z.number().int().min(1).max(90),flashFrames:z.number().int().min(2).max(60),maxScale:z.number().min(1).max(12),rayCount:z.number().int().min(4).max(80)});export type BloomFlashTransitionProps=z.infer<typeof bloomFlashTransitionSchema>;export const bloomFlashTransitionDurationFrames=60;
export const defaultBloomFlashTransitionProps={coreColor:"#ffffff",glowColor:"#35eaff",centerX:50,centerY:50,peakFrame:28,flashFrames:12,maxScale:7,rayCount:32} as const;export const bloomFlashTransitionPatterns:Record<string,BloomFlashTransitionProps>={cyanOverexposure:{...defaultBloomFlashTransitionProps},amberImpact:{...defaultBloomFlashTransitionProps,glowColor:"#ff9d2e",centerX:58,centerY:44,peakFrame:24,maxScale:9,rayCount:46}};
